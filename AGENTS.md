# AGENTS.md
## Setup commands
```
bun install
```

## 1. Web 前端 (`apps/web`)
- **包管理**：使用 `bun` 进行依赖管理。
- 编写页面要使用 ui-ux-pro-max 技能
- 所有涉及到文案的地方都要中英双译文支持，请务必在 locales 目录下补全相关译文映射

## 2. 服务端 server (`apps/api`)
- **包管理**：使用 `bun` 进行依赖管理。